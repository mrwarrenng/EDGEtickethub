[build]
  publish = "."
  functions = "netlify/functions"
  command = "npm install"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "ADMIN_JWT_SECRET,ADMIN_PASSPHRASE,ADMIN_RECOVERY_CODE"

[functions]
  node_bundler = "esbuild"

[functions."keepalive"]
  schedule = "@daily"
